This folder contains scripts that replace the legacy '_segment1corpus.text' script.

- Main script is segment_one_corpus.sh that you can use with same
  arguments as previously.

- AG.sh, dibs.sh, ngrams.sh and TPs.sh each launch a single
  segmentation algorithm.
